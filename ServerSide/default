upstream flask_serv {
    server unix:/scratch/stm.sock;
    #server /tmp/stm.sock;
}

server {
    listen 80;
    server_name _;

    location / {
        uwsgi_pass flask_serv;
        include uwsgi_params;
    }

}